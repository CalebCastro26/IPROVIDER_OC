import { FCLLayout } from "@ui5/webcomponents-react";
import {data} from '../../data'

export const defaultState = {
    appName: "Ordenes de Compra",
    layout: FCLLayout.OneColumn,
    data: data
};
